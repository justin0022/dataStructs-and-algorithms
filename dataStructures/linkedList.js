const createNode = val => ({
  val,
  next: null
})

const createLinkedList = () => {
  let head = null
  return {
    insert: function (val) {

    },
    delete: function (val) {

    },
    find: () => {

    },
    contains: () => {

    }
  }
}

module.exports = createLinkedList
